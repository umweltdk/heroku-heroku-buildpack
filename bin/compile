#!/usr/bin/env bash

set -e            # fail fast
set -o pipefail   # don't ignore exit codes when piping output
# set -x          # enable debugging

bp_dir=$(cd $(dirname $0); cd ..; pwd)

/bin/sh $bp_dir/bin/bundler.sh
/bin/sh $bp_dir/bin/nodejs.sh
/bin/sh $bp_dir/bin/grunt.sh