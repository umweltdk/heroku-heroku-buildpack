#!/usr/bin/env bash

PACKAGE_JSON=$(find $1 -iname "package.json" -maxdepth 1)
GEMFILE=$(find $1 -iname "gemfile" -maxdepth 1)

if [ -n $PACKAGE_JSON -a -n $GEMFILE ]; then
  echo "UmweltBuildpack"
  exit 0
else
  exit 1
fi
